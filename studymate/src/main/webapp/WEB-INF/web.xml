<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>StudyMate</display-name>

    <!-- Página inicial -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Página de erro 404 -->
    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/views/erro/404.jsp</location>
    </error-page>

    <!-- Página de erro 500 -->
    <error-page>
        <error-code>500</error-code>
        <location>/WEB-INF/views/erro/500.jsp</location>
    </error-page>

    <!-- Encoding UTF-8 para todos os requests -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>com.studymate.filter.EncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Filtro de autenticação — bloqueia acesso sem login -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>com.studymate.filter.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/app/*</url-pattern>
    </filter-mapping>

    <!-- Tempo de sessão: 30 minutos -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

</web-app>
